package com.erd.cowork.agent.provider.openai;

/**
 * A placeholder-comment omission detected in a generated HTML artifact.
 *
 * @param commentText the raw comment text (trimmed) that matched a placeholder pattern
 */
public record CodeOmissionFinding(String commentText) {}
